# 天猫羊毛换装互动项目交付说明

本压缩包基于甲方 GitHub 仓库最新 `main` 版本制作，已包含本次需求修改和构建后的 `dist` 运行包。

## 本次已调整

- 双手叉腰、肩髋膝进入新版人形框后，保持约 0.9 秒完成姿势锁定。
- 锁定后衣服出现；之后动作变化不会隐藏衣服。
- 人只需进入约三分之二身体，脚踝不再是锁定硬条件。
- 摄像头识别食指“比 1”，指向“羊毛雨”UI 图标停留 1 秒：云朵滑入并开始下雨。
- 手指移开后再指向图标停留 1 秒：停止下雨并让云朵滑出。
- 鼠标或触屏直接点击同一图标也可开关。
- 保留羊头绳拖拽/捏住下拉操作。
- 移除云朵点击关闭和羊毛雨 UI 捏合开关。

## 验收结果

- 已与 GitHub 远端 `main` 核对，基线提交为 `8160690`。
- 已使用仓库自带的 `public/test/pose-hands-on-hips.png` 实际跑姿势识别：状态达到 `ready`，上衣和下装实际绘制到人物身上。
- 已用真实鼠标事件连续点击“羊毛雨”图标：第一次云朵滑入并下雨，第二次停雨并滑出。
- 已验证食指指向坐标的停留触发：停留一次打开，移开后再停留一次关闭。
- 已执行 `npm run build`，TypeScript 检查和 Vite 生产构建通过。

## 源码开发

在项目根目录执行：

```bash
npm ci
npm run dev
```

浏览器打开 `http://localhost:5173/`。

## 直接运行

`dist/` 是已经构建好的运行包。不能直接双击 `dist/index.html`，需要通过 HTTP 服务打开：

```bash
cd dist
python3 -m http.server 8080
```

然后访问 `http://localhost:8080/`。摄像头权限需要使用 `localhost` 或 HTTPS。

本次交付构建命令：

```bash
npm run build
```
